generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider  = "postgresql"
  url       = env("DATABASE_URL")
  directUrl = env("DIRECT_URL")
}

model UserOrder {
  id        String   @id @default(cuid())
  email     String?
  
  // Package details
  countryCode  String
  countryName  String
  packageCode  String
  packageName  String
  days         Int?
  amountUSD    Float
  
  // Stripe
  stripeSessionId       String   @unique
  stripePaymentIntentId String?
  paymentStatus         String   @default("pending") // pending | paid | failed
  
  // eSIM Access
  esimStatus     String?  // processing | completed | failed
  esimOrderNo    String?
  esimTranNo     String?
  iccid          String?
  qrCodeUrl      String?
  activationCode String?
  smdpAddress    String?
  matchingId     String?
  
  // Raw data
  rawStripeSession String? @db.Text // JSON string
  rawEsimResponse  String? @db.Text // JSON string
  
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
